import { Button } from '../../components/ui/button';

const Checkout = () => {
    return (
        <div>
            <p>this this checkout page</p>
            <Button variant="outline">Cash on Delivery</Button>
        </div>
    );
};

export default Checkout;